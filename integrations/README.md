# Интеграции

Внешние модули и отдельные проекты, связанные с NewFormat Tour.

## roja-id

Модуль распознавания лиц. Отдельный проект (Node.js backend + frontend) с собственным `package.json` и зависимостями.
